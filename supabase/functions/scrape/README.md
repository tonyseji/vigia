# Función `scrape`

Recibe una URL de producto y devuelve título, imagen y precio.

Todavía vacía: el extractor se porta aquí en la **fase 3** desde el primer
commit del repositorio (`git show a313915 -- supabase/functions/muebles/extract.ts`),
añadiendo tests de las funciones puras (`parsePrice`, parseo de JSON-LD) que
hoy no están versionados.
